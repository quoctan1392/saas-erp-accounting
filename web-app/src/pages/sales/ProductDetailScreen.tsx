// Deleted: ProductDetailScreen has been replaced by ProductDetailConfig (route-backed).
// Keeping a harmless stub to avoid accidental imports breaking the build.

export default null as unknown as React.FC;
